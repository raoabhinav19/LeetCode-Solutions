class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        
        output = 0
        
        for elem in nums:
            if len(str(elem)) % 2 == 0:
                output += 1
        
        return output
