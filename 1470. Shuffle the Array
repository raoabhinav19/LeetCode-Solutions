class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        
        k = []
        for i in range (0,n):
            k.append(nums[i])
            k.append(nums[n+i])
        return k
