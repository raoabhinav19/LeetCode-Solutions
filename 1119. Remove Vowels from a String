class Solution:
    def removeVowels(self, s: str) -> str:
        
        output_string = ""
        
        for letter in s:
            if letter not in ['a','e','i','o','u']:
                output_string += letter
        return(output_string)
