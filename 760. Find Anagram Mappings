class Solution:
    def anagramMappings(self, nums1: List[int], nums2: List[int]) -> List[int]:
        
        index_list = []
        
        for i in range(len(nums1)):
            
            index_list.append(nums2.index(nums1[i]))
        
        return index_list
